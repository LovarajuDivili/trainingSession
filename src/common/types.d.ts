export interface MenuItemType {
  label: string;
  path: string;
}

export interface Employee {
  id: string;
  name: string;
  email: string;
  role: string;
  joinDate: string;
  skills: string[];
  currentDate: string;
}
