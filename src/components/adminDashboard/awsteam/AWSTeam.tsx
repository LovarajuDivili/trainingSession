import React from "react";
import { Box, Typography } from "@mui/material";

const AWSTeam: React.FC = () => {
  return (
    <Box sx={{ mt: 25, textAlign: "center" }}>
      <Typography variant="h5" sx={{ fontWeight: 600, color: "gray" }}>
        AWS Team section is in progress...
      </Typography>
    </Box>
  );
};

export default AWSTeam;
